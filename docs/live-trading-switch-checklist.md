# 实盘切换前置检查单（Live-Trading Switch Checklist）

> 状态：评审稿（T-07 准备项，**不执行实盘**）
> 更新：2026-08-13
> 阶段约束：本阶段不接入真实交易。本检查单用于**纸面完备后**、切换实盘前的逐项核验；切换动作需另行立项并经确认。

---

## 0. 切换前提

- [ ] 纸面交易全流程稳定运行（T-06 5 日演练通过）
- [ ] 定价闭环（实时行情→滑点→成交价）已验证（T-02）
- [ ] 回归测试通过，`ci_gate.sh` 全绿
- [ ] 本检查单各项已核验并记录

---

## 1. 风控参数校验（切换前必查）

| # | 检查项 | 现状 | 缺口 / 待办 | 验收方式 |
| --- | --- | --- | --- | --- |
| 1.1 | 单票集中度上限（`RiskConfig.max_pct_per_stock`） | 30% | 实盘建议收紧至 15-20%，需评审 | 配置评审 + RMS 单测 |
| 1.2 | 单笔现金占比（`max_pct_cash_per_buy`） | 50% | 实盘建议 20-30% | 配置评审 |
| 1.3 | 当日亏损熔断（`max_daily_loss_pct`） | 5% | 实盘需按资金规模核定 | 熔断测试 |
| 1.4 | 三级熔断阈值（soft/hard/liquidation） | soft 3%/hard 5%/liq 8% | 实盘按账户风险偏好复核 | 熔断单测 |
| 1.5 | 滑点模型（`FeeModel.slippage_bps`） | 默认 bps | 实盘按券商真实滑点校准 | 回放对比 |
| 1.6 | 极端行情应对（暂停 buy + 禁用市价单） | 已实现 | 实盘触发后须人工确认 | 演练 + 评审 |

## 2. 券商接入（sandbox 验证，不开真实资金）

| # | 检查项 | 现状 | 缺口 / 待办 | 验收方式 |
| --- | --- | --- | --- | --- |
| 2.1 | `BrokerRouter.resolve_by_account` 路由 | 已修（支持注入，5 单测通过） | — | `tests/test_broker_router.py` |
| 2.2 | 券商 adapter（eastmoney/longbridge）下单/撤销/回报 | `EastMoneyBroker` 存在，`ENABLE_EASTMONEY_PATCH=false` 未启用 | 接入券商 sandbox 验证一单真实链路 | sandbox 实测 + 回报对账 |
| 2.3 | 订单幂等（`PaperOrder.version` 乐观锁） | 已实现 | sandbox 并发下单幂等验证 | 并发单测 |
| 2.4 | 券商断连 fallback（`BROKER_FAILOVER_TO_PAPER_ON_DISCONNECT`） | true | 实盘断连时自动切 paper 需演练 | 断线演练 |
| 2.5 | 订单状态机（pending→filled/canceled/rejected）映射券商回报 | 纸面状态机已有 | 映射真实券商回报字段 | sandbox 对账 |

## 3. 订单生命周期与对账

| # | 检查项 | 现状 | 缺口 / 待办 | 验收方式 |
| --- | --- | --- | --- | --- |
| 3.1 | 部分成交 | 纸面支持 | 实盘部分成交回报合并 | sandbox 验证 |
| 3.2 | 撤单/改单竞态 | `version` 乐观锁 | 实盘并发撤改 | 并发测试 |
| 3.3 | 日终对账（本地持仓 vs 券商持仓） | 无 | 需新增对账任务 | 对账脚本 |
| 3.4 | 资金/冻结一致性 | 纸面 `freeze_cash` | 实盘资金核对 | 对账 |

## 4. 权限与告警

| # | 检查项 | 现状 | 缺口 / 待办 | 验收方式 |
| --- | --- | --- | --- | --- |
| 4.1 | 实盘操作确认流程 | 无 | 需人工二次确认（大单/风控触发） | 流程评审 |
| 4.2 | 告警通道（下单/成交/风控/断连） | 通知服务已有 | 实盘关键事件必达 | 通知测试 |
| 4.3 | 审计日志（谁/何时/何操作） | 纸面有信号/订单审计 | 实盘操作审计 | 日志审查 |
| 4.4 | 权限边界（仅授权账户可下单） | `require_login` + 账户归属 | 实盘账户需额外授权 | 权限测试 |

## 5. 回滚与应急预案

| # | 检查项 | 现状 | 缺口 / 待办 | 验收方式 |
| --- | --- | --- | --- | --- |
| 5.1 | 一键切回 paper | `resolve_by_account` fallback | 需"实盘→纸面"开关 | 演练 |
| 5.2 | 熔断人工接管 | CircuitBreaker | 实盘熔断后人工确认再恢复 | 演练 |
| 5.3 | 极端行情应急 | ExtremeMarket 暂停 buy | 实盘触发后停市价单 | 演练 |

---

## 6. 明确不做（本阶段）

- 不开启真实券商实盘下单/资金
- 不部署实盘券商凭证到生产（`BROKER_EASTMONEY_USER/PASSWORD` 保持占位）
- 不引入真实资金与真实账户数据

---

## 7. 决策记录

| 决策 | 结论 | 日期 | 备注 |
| --- | --- | --- | --- |
| 本阶段接入实盘 | 否 | 2026-08-13 | 纸面完备后再切换 |
| 固收模块（T-04） | 做最小闭环 | 2026-08-13 | 已落地数据源+计算+API |
| 实盘切换立项 | 待纸面完备 + 本检查单核验后 | — | — |

---

*本检查单随 `docs/paper_trading_next_phase_plan.md`（T-07）维护；核验结果逐项记录，切换动作需另行确认。*
